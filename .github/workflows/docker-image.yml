name: Docker Image CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  DOCKER_REGISTRY_USERNAME: ${{ secrets.TINKMASTER_DOCKER_HUB_ACCOUNT_NAME }}
  DOCKER_REGISTRY_PASSWORD: ${{ secrets.TINKMASTER_DOCKER_HUB_ACCOUNT_PASSWORD }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: make
